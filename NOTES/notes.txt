@ViewChild
@HostBinding
@Output, @Input
custom directive
EventEmitter

Dependency Injection
    return a copy of array using arr.slice() for security

Routing
    redirectTo, pathMatch, children
    routerLink, routerLinkActive
    ActivatedRoute, 
    router.navigate(), relativeTo: 

Observable
    interval(), Observable.create(observer=>)
    operators
        map, filter, tap, 
    Subject
        .next(),  
    
